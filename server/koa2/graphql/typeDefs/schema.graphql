
# import User from 'user.graphql'
scalar Date

scalar ObjectId

type Root {
    _id: ObjectId
    user(nick: String): User
    users: [User!]!
}

type Query {
    root: Root
}

type Mutation {
    Root(_id: ID): Root
}